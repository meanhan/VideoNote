<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="AppTheme.NoActionBar">
        <!-- App bar 背景色 -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <!-- 状态栏背景色 -->
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <!-- 各控制元件被勾选 (checked) 或是选定 (selected) 的颜色 -->
        <item name="colorAccent">@color/colorAccent</item>
        <!-- 溢出菜单图标颜色-->
        <item name="colorControlNormal">@android:color/black</item>
        <!-- 溢出菜单图标自定义-->
        <item name="android:actionOverflowButtonStyle">@style/OverflowButtonStyle</item>
        <!-- 设置系统状态栏透明 5.0以后-->
        <item name="android:statusBarColor">@color/colorTransparent</item>
        <!-- 设置系统状态栏透明 6.0以后-->
        <!--设置为true时,当statusbar的背景颜色为淡色时,statusbar的文字颜色会变成深色-->
        <item name="android:windowLightStatusBar">true</item>

        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>

    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <!-- HomeFragment toolbar弹出菜单样式 -->
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:textSize">15sp</item>
        <!--<item name="android:colorBackground">@color/colorPrimary</item>-->
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="actionOverflowMenuStyle">@style/OverflowMenuStyle</item>
        <item name="android:dropDownListViewStyle">@style/AppDropDownListViewStyle</item>
    </style>

    <style name="OverflowMenuStyle" parent="Widget.AppCompat.Light.PopupMenu.Overflow">
        <!--把该属性改为false即可使menu位置位于toolbar之下-->
        <item name="overlapAnchor">false</item>
        <!--menu与toolbar之间间隔-->
        <item name="android:dropDownVerticalOffset">1dp</item>
        <!--popupMenu圆角背景-->
        <item name="android:popupBackground">@drawable/menu_popup_bg</item>
    </style>

    <style name="AppDropDownListViewStyle" parent="Widget.AppCompat.ListView.DropDown">
        <item name="android:showDividers">middle</item>
        <item name="android:divider">@color/colorWhite</item>
        <item name="android:dividerHeight">0.5dp</item>
        <!--分割线左右间距-->
        <item name="android:paddingStart">20dp</item>
        <item name="android:paddingEnd">20dp</item>
    </style>

    <!-- ListFragment toolbar弹出菜单样式 -->
    <style name="ListTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:textSize">15sp</item>
        <item name="android:textColorPrimary">@color/textColorGray</item>
        <item name="actionOverflowMenuStyle">@style/WhiteOverflowMenuStyle</item>
        <item name="android:dropDownListViewStyle">@style/WhiteDropDownListViewStyle</item>
    </style>

    <style name="WhiteOverflowMenuStyle" parent="Widget.AppCompat.Light.PopupMenu.Overflow">
        <item name="overlapAnchor">false</item>
        <item name="android:dropDownVerticalOffset">1dp</item>
        <item name="android:popupBackground">@drawable/menu_popup_white_bg</item>
    </style>

    <style name="WhiteDropDownListViewStyle" parent="Widget.AppCompat.ListView.DropDown">
        <item name="android:showDividers">middle</item>
        <item name="android:divider">@color/textColorGray</item>
        <item name="android:dividerHeight">0.5dp</item>
        <item name="android:paddingStart">8dp</item>
        <item name="android:paddingEnd">8dp</item>
    </style>

    <style name="OverflowButtonStyle" parent="Widget.AppCompat.Light.ActionButton.Overflow">
        <item name="android:src">@drawable/icon_more_gray</item>
    </style>

    <style name="ApiTheme" parent="AppTheme.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowShowWallpaper">true</item>
    </style>

    <style name="MomentTheme" parent="AppTheme.NoActionBar">
        <item name="colorPrimaryDark">@color/colorGrayLight</item>
        <item name="android:statusBarColor">@color/colorTransparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowTranslucentStatus">true</item>
    </style>

</resources>
